# 宝宝学数字：最终修复与 QA 报告

**总体状态：ENV_UNVERIFIED**

源码修复已完成；当前运行环境未能完成依赖型检查，需在可访问 npm 的环境执行验证命令。

## 已修复

1. 静音状态进入“听声音找数字”时明确提示，并提供“打开声音并开始”。
2. 答对庆祝层不再使用点击穿透，动画期间会拦截底层按钮。
3. 320px 窄屏重新压缩标题、图标、箭头、间距和字号，保持无横向滚动。
4. 首次使用不再显示虚假的“继续学习数字 1”和“上次学到这里”。
5. 使用 `useSyncExternalStore` 读取浏览器进度，首屏以稳定占位显示，避免先归零再闪回。
6. localStorage 升级为 v2：支持旧数据迁移、去重、范围清洗、损坏恢复和存储不可用时的会话内存回退。
7. 单文件业务原型已移除，功能只维护一套正式 Next.js 源码。
8. PWA 移除强制竖屏，补齐 Manifest、桌面图标、Service Worker、旧缓存清理和离线回退。
9. `preview` 改为项目内零依赖静态服务器，不再使用 `npx serve@latest`。
10. 数字卡片滑动后不再额外触发旧数字朗读。
11. 新增单元测试和 Playwright 交互测试，覆盖首次状态、进度持久化、静音听力、遮罩误触、数字 100 和左右滑动。

## 工程文件

- `package-lock.json`：当前环境未生成
- 静态导出 `out/`：当前环境未生成
- QA 截图：当前环境未生成

## 自动检查

| 检查 | 命令 | 结果 |
|---|---|---|
| 依赖安装 | `npm install / npm ci` | ⚪ 无结果 |
| TypeScript 类型检查 | `npm run typecheck` | ⚪ 当前环境未运行 |
| ESLint | `npm run lint` | ⚪ 当前环境未运行 |
| 单元测试 | `npm run test:unit` | ⚪ 当前环境未运行 |
| Next.js 静态构建 | `npm run build` | ⚪ 当前环境未运行 |
| Playwright 交互测试 | `npx playwright test` | ⚪ 当前环境未运行 |
| QA 截图采集 | `Playwright Chromium` | ⚪ 当前环境未运行 |
| TS/TSX 语法扫描 | Babel Parser | ⚪ 当前环境未运行 |

## 已加入的交互验收用例

- 首次打开不伪造学习记录。
- 320×568、390×844、1024×900 页面无横向溢出。
- 学到数字 7 后刷新，进度和上次位置仍存在。
- 静音进入听力游戏时显示开声提示。
- 答对遮罩拦截底层点击并自动进入下一题。
- 数字 100 显示“一百”、100 个物品和 10 个数量组。
- 数字卡片支持左右滑动。

## 验证方式

当前会话没有 Browser 插件，因此按前端测试流程使用 Playwright Chromium 作为回退验证方式。

## 真机验收边界

中文语音音色、iPhone/iPad“添加到主屏幕”和安卓桌面安装行为依赖真实设备及浏览器。自动化测试不能完全替代 iOS Safari 与安卓 Chrome 的最终真机检查。
